#include <stdio.h>

/**
 *main - print "with proper grammar, but the outcome is a piece of art,\n"
 *dont forget to put the new line
 *return: Always 0 (succes)
 */
int main(void)
{ 
        printf("with proper grammar, but the outcome is a piece of art,\n");
        return (0);
}
