#include "main.h"

/**
* _abs - computes the absolute value of an integer
* @n: the integer check
* Return: the value of int
*/

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else if (n < 0)
{

return (-n);
}

